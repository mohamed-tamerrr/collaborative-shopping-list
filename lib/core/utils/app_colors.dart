import 'package:flutter/material.dart';

class AppColors {
  // Navy Blue shades (primary text/background colors)
  static const Color navyBlue = Color(0xFF0A2647);
  static const Color mediumNavy = Color(0xFF144272);
  static const Color lightNavy = Color(0xFF205295);

  // Orange shades (primary action/accent colors)
  static const Color orange = Color(0xFFFF7F3E);
  static const Color lightOrange = Color(0xFFFFA364);

  // Neutral colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Color(0xFF9CA3AF);
  static const Color lightGrey = Color(0xFFE5E7EB);
}
